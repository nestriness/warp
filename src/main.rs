
use gst::prelude::*;

use std::path::PathBuf;
use std::sync::{Arc, Mutex};

use anyhow::Error;

fn main() -> Result<(), Error> {
    // gst::init()?;

    // gstfmp4::plugin_register_static()?;

    
    Ok(())
}
